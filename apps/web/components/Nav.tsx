"use client";
import { useEffect, useState } from "react";
import { waLink, WA_PREFILL } from "../lib/whatsapp";
const links = [["How it works", "how-it-works"], ["Features", "features"], ["Supported countries", "countries"], ["Trust & safety", "trust"]];
export default function Nav() { const [open, setOpen] = useState(false); const [scrolled, setScrolled] = useState(false); useEffect(() => { const onScroll = () => setScrolled(window.scrollY > 20); window.addEventListener("scroll", onScroll); return () => window.removeEventListener("scroll", onScroll); }, []); return <header className={("sticky top-0 z-50 transition " + (scrolled ? "bg-white/95 text-[#0F0A1E] shadow-lg shadow-[#0F0A1E]/10 backdrop-blur" : "bg-[#0F0A1E] text-white"))}><nav className="section-wrap flex h-[72px] items-center justify-between" aria-label="Main navigation"><a href="#top" className="font-display text-[21px] font-bold tracking-[-.04em]">infinitswap<span className="text-[#FF6B6B]">.</span></a><button className="rounded-lg p-2 md:hidden" aria-label="Toggle navigation" aria-expanded={open} onClick={() => setOpen(!open)}><span className="text-xl">☰</span></button><div className={(open ? "flex " : "hidden ") + "fixed inset-x-0 top-[72px] min-h-[calc(100vh-72px)] w-full flex-col gap-6 bg-[#0F0A1E] p-8 text-white md:static md:flex md:min-h-0 md:w-auto md:flex-row md:items-center md:gap-8 md:bg-transparent md:p-0 md:text-inherit"}>{links.map(([label, id]) => <a key={id} href={"#" + id} onClick={() => setOpen(false)} className="text-sm font-medium opacity-80 transition hover:opacity-100">{label}</a>)}<a href={waLink(WA_PREFILL.start)} className="rounded-full bg-[#6C3FE8] px-6 py-3 text-center text-sm font-semibold text-white transition hover:bg-[#4A2DB0] focus:outline-none focus:ring-2 focus:ring-[#FF6B6B] focus:ring-offset-2">Start using Infinitswap</a></div></nav></header>; }
